<main>
    {{#if latitude}}
    	{{google-maps latitude=latitude longitude=longitude markers=markers}}
    {{/if}}
    <div class="testing">
	    <h3>Do you want to Create a Profile?</h3>
	    	<button class="btn">{{#link-to "new_user"}}<span class="glyphicon glyphicon-plus"></span>Make a Profile{{/link-to}}</button>
    </div>
</main>

